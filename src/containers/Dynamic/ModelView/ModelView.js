import React, { Component } from "react";
import classes from "./ModuleView.module.css";

class ModuleView extends Component {
  /**
   * view other ppls published models
   * 1. fetch all models from published db
   * 2. show all models in grid format
   * 3. have a search bar by name
   * 4. have a link to mess around with the sliders
   * 5. have a link to download the model to ur folder and edit there 
   * 6. sort by relevance/date/popularity
   *
   */

  render() {
    return (
      <div>
       
      </div>
    );
  }
}

export default ModuleView;
